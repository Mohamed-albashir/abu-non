[build]
  command = "npm run build"
  publish = "dist"

# Optional: set a redirect fallback here too, but we already have `_redirects`.
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
